# ${{ values.name }}

${{ values.description }}

| | |
|---|---|
| Runtime | Python |
| Memória | ${{ values.memory }} MB |
| Timeout | ${{ values.timeout }}s |

Gerado pelo template **Lambda** do Backstage. Handler em `handler.py`.
