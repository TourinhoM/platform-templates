# Lambda ${{ values.name }}
quarkus.lambda.handler=handler

# Config de deploy (referência — aplicada pelo IaC, não pelo runtime)
# memory  = ${{ values.memory }} MB
# timeout = ${{ values.timeout }} s
