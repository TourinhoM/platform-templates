# ${{ values.name }}

${{ values.description }}

| | |
|---|---|
| Runtime | Quarkus (Java) |
| Memória | ${{ values.memory }} MB |
| Timeout | ${{ values.timeout }}s |

Gerado pelo template **Lambda** do Backstage. Handler em
`src/main/java/labs/tourinho/LambdaHandler.java`.
