apiVersion: v2
name: ${{ values.name }}
description: ${{ values.description }}
type: application
version: 0.1.0
appVersion: "0.1.0"
# Consome o arquétipo central por versão (bump via Renovate).
dependencies:
  - name: ${{ values.archetype }}
    version: "2.4.4"
    repository: oci://ghcr.io/tourinhom/charts
