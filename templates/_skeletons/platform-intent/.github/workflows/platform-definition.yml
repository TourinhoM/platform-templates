name: Platform Definition

# Valida o contrato (platform.yaml) no PR antes do merge.
on:
  pull_request:
    branches:
      - main
    paths:
      - platform.yaml

jobs:
  validate:
    permissions:
      contents: read
    uses: TourinhoM/org-ci-platform/.github/workflows/service-definition.yml@main
