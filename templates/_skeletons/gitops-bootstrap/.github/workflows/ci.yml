# CI do repo de deploy: smoke de `kustomize build`.
name: CI

on:
  push:
  workflow_dispatch:

jobs:
  ci:
    permissions:
      contents: read
    uses: TourinhoM/org-ci-platform/.github/workflows/ci-gitops.yml@main
